{
  "name": "walkie-talkie",
  "version": "1.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "bun install --no-summary && bun server.ts",
    "broker": "bun broker.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
